name: jitterentropy

services:
    rngd:
        build:
            context: https://github.com/tcely/jitterentropy-rngd.git#patch-1
        cap_add:
            - SYS_ADMIN
        cap_drop:
            - ALL
        network_mode: none
        restart: always
